{
  "name": "@estate/auth-guard",
  "version": "0.0.1",
  "private": true,
  "description": "Shared PEP middleware: real cross-service session verification (introspection against identity's /v1/auth/session) and step-up freshness. Replaces the M2 gateway-injected header trust; the SessionVerifier interface is the seam for the future OIDC/JWT local-verify swap.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@estate/contracts": "workspace:*",
    "@nestjs/common": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "jest": "catalog:",
    "reflect-metadata": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
