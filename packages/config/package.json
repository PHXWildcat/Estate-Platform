{
  "name": "@estate/config",
  "version": "0.0.1",
  "private": true,
  "description": "Shared TypeScript and Jest configuration for the estate platform workspace.",
  "files": [
    "tsconfig.base.json",
    "jest.js",
    "ci-guard.js",
    "ci-guard.d.ts"
  ],
  "scripts": {
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "jest": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
