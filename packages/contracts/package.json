{
  "name": "@estate/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Shared contracts: Kafka topic registry, domain event schemas (zod), audit action catalog. Payloads carry entity IDs and enums only — never PII.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "zod": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "jest": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
