{
  "name": "@estate/kafka",
  "version": "0.0.1",
  "private": true,
  "description": "Kafka producer/consumer wrappers, reserved in docs/04 since M1 and built in M8. Until now every service carried its own byte-identical copy of the audit producer — seven of them, all sharing one availability bug. The topic REGISTRY stays in @estate/contracts, where producers and consumers already import it from; this package owns transport only.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "@estate/audit-emitter": "workspace:*",
    "kafkajs": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "jest": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
