# Generated by `next dev`/`next build`; references .next/types, so a checked-in
# copy would break typecheck on a clean clone. src/types/globals.d.ts carries
# the declarations a build-less typecheck needs.
next-env.d.ts
