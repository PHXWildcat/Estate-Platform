{
  "name": "@estate/web",
  "version": "0.0.1",
  "private": true,
  "description": "Next.js frontend: auth flows (register, login, TOTP, step-up), design tokens, dark mode, WCAG AA+. Auth lives in httpOnly cookies set by the BFF — this app never holds tokens.",
  "scripts": {
    "build": "next build",
    "dev": "next dev",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "test": "jest"
  },
  "dependencies": {
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@tailwindcss/postcss": "catalog:",
    "@testing-library/jest-dom": "catalog:",
    "@testing-library/react": "catalog:",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "jest": "catalog:",
    "jest-environment-jsdom": "catalog:",
    "postcss": "catalog:",
    "tailwindcss": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
