// Side-effect stylesheet imports (e.g. app/globals.css). After a build, the
// generated next-env.d.ts provides this too; declaring it here keeps
// `tsc --noEmit` green on a clean checkout with no .next directory.
declare module '*.css';
