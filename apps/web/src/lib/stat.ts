/**
 * Uppercase micro-label above a stat figure — ONE spelling of the stat-tile
 * label style. Hoisted in M24 PR3, when the dashboard would otherwise have
 * been the fourth verbatim copy (AssetsPanel, ReadinessPanel, DocumentsPanel
 * each carried their own).
 */
export const STAT_LABEL =
  'text-[0.6875rem] font-semibold uppercase tracking-[0.08em] text-ink-muted';
