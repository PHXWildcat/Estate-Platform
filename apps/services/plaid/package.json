{
  "name": "@estate/service-plaid",
  "version": "0.0.1",
  "private": true,
  "description": "Plaid isolating service: link/exchange, account sync, webhooks, per-item revocation (docs/01 §2.3, docs/03 TB5). Access tokens decrypt ONLY inside this service, under its own KEK alias — the KMS grant is the isolation chokepoint.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest --runInBand",
    "dev": "node dist/main.js"
  },
  "dependencies": {
    "@aws-sdk/client-kms": "catalog:",
    "@estate/audit-emitter": "workspace:*",
    "@estate/authz": "workspace:*",
    "@estate/contracts": "workspace:*",
    "@estate/crypto": "workspace:*",
    "@estate/db": "workspace:*",
    "@estate/kms-aws": "workspace:*",
    "@nestjs/common": "catalog:",
    "@nestjs/core": "catalog:",
    "@nestjs/platform-express": "catalog:",
    "kafkajs": "catalog:",
    "pg": "catalog:",
    "reflect-metadata": "catalog:",
    "rxjs": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@nestjs/testing": "catalog:",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "@types/pg": "catalog:",
    "@types/supertest": "catalog:",
    "jest": "catalog:",
    "supertest": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
