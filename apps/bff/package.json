{
  "name": "@estate/bff",
  "version": "0.0.1",
  "private": true,
  "description": "GraphQL BFF: the only GraphQL surface (docs/01 §7). Persisted operations enforced in production; auth via httpOnly cookies; proxies to internal service REST APIs.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "dev": "node dist/main.js"
  },
  "dependencies": {
    "@estate/contracts": "workspace:*",
    "@graphql-yoga/plugin-persisted-operations": "catalog:",
    "@nestjs/common": "catalog:",
    "@nestjs/core": "catalog:",
    "@nestjs/platform-express": "catalog:",
    "graphql": "catalog:",
    "graphql-yoga": "catalog:",
    "reflect-metadata": "catalog:",
    "rxjs": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@estate/config": "workspace:*",
    "@nestjs/testing": "catalog:",
    "@types/jest": "catalog:",
    "@types/node": "catalog:",
    "@types/supertest": "catalog:",
    "jest": "catalog:",
    "supertest": "catalog:",
    "ts-jest": "catalog:",
    "typescript": "catalog:"
  }
}
